

const array = [1, 5, 2, 6, 3, 7, 4]

function solution(array, commands) {
    
    const answer = [];
    return answer;
}
console.log(array.slice(i-1,j).sort())
const a = array.slice(i-1,j).sort();
console.log(a[k])

function commands(i,j,k){
        let arr =[];
        for(let b=0 ; b< array.length; b++){
            const sort = array[b].slice(i,j).sort();
            const result = sort[k];
            result.push(arr)
        }
      
                    }
                   console.log(commands(1,2,3))
